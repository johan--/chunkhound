# Handover: chunkhound - 2026-03-07 08:50

## Current Task
Elixir parser stress testing — indexing complete, PR prep for tomorrow.

## Status
- [x] All 7 open-source Elixir repos indexed with embeddings (48,762 chunks, 48,573 embeddings)
- [x] Elixir stdlib indexed: 659 files, 21,434 embeddings, 99.2% coverage
- [x] Search quality validated: 15 semantic + 7 regex queries, 21/22 relevant results
- [x] Stress test report updated in `ai-steering/chunkhound/elixir-stress-test-results.md`
- [x] `.chunkhound.json` for elixir stdlib updated: added `**/*.txt` exclude (Unicode data files)
- [ ] MCP integration tests against Oban, Ecto, Absinthe (tomorrow)
- [ ] PR creation
- Current branch: `sandbox`
- Last commit: `083e6b6 - chore: session handover - Elixir stress test indexing in progress`

## Immediate Next Steps
1. Run MCP integration tests (see plan below)
2. Create PR from `sandbox` → `main`

## MCP Integration Test Plan

Start Claude Code sessions with ChunkHound MCP server pointed at each repo. Verify the MCP tools return useful, actionable results for an AI assistant.

### Oban (OTP patterns, behaviours, plugins)

| # | Type | Query | What good looks like |
|---|------|-------|---------------------|
| 1 | semantic | "how does job retry and backoff work?" | `worker.ex` or `engine.ex` retry logic |
| 2 | semantic | "queue supervision and process tree" | `oban.ex` or `queue/supervisor.ex` |
| 3 | semantic | "telemetry events and instrumentation" | `telemetry.ex` event definitions |
| 4 | regex | `def\s+perform` | Worker perform/1 implementations |
| 5 | regex | `defmodule.*Plugin` | Plugin modules |

### Ecto (DSL, macros, query composition)

| # | Type | Query | What good looks like |
|---|------|-------|---------------------|
| 1 | semantic | "changeset validation and casting" | `changeset.ex` validate_* functions |
| 2 | semantic | "composable query with joins and preloads" | `query.ex` or `query/api.ex` |
| 3 | semantic | "schema field types and associations" | `schema.ex` has_many/belongs_to macros |
| 4 | regex | `defmacro __schema__` | Schema compilation macro |
| 5 | regex | `def\s+changeset` | Changeset function definitions |

### Absinthe (macros, GraphQL DSL)

| # | Type | Query | What good looks like |
|---|------|-------|---------------------|
| 1 | semantic | "GraphQL type resolution and middleware" | `resolution.ex` or `middleware.ex` |
| 2 | semantic | "schema definition macros for queries and mutations" | `schema/notation.ex` |
| 3 | semantic | "subscription pubsub and real-time updates" | `subscription/` modules |
| 4 | regex | `defmacro\s+field` | Field definition macro |
| 5 | regex | `defmodule.*Resolver` | Resolver modules |

### How to run

```bash
# Start MCP server pointed at a repo
uv run chunkhound mcp --db /home/johan/workbench/elixir/{repo}/.chunkhound

# Or use HTTP mode
uv run chunkhound mcp http --port 5173 --db /home/johan/workbench/elixir/{repo}/.chunkhound
```

Configure Claude Code's MCP settings to use the server, then ask the queries naturally in conversation.

### Pass criteria
- Semantic results are in the right file/module (not just keyword matches)
- Regex results return actual code, not just comments
- Import chunks appear in results when relevant
- No crashes or DB lock errors during MCP session

## Blockers / Open Questions
- GPU overheats at sustained load — index one repo at a time if re-indexing needed
- Rerank 404s from Ollama are noisy but non-fatal
- CLI quirk: `search semantic "query"` treats `semantic` as query — use `search "query"` instead

## Key Decisions Made
- **Elixir stdlib excludes:** `descr.ex` (parse timeout), `**/*.txt` (Unicode data files — 3.7MB of non-code)
- **MCP test repos:** Oban (OTP), Ecto (DSL), Absinthe (macros) — covers three distinct Elixir patterns
- **Skip for MCP tests:** Finch/Swoosh (too small), Livebook (less Elixir-focused), stdlib (parse issues add noise)

## Context the Next Session Needs
- All repos at `/home/johan/workbench/elixir/` with `.chunkhound.json` configs
- Embedding provider: Ollama `nomic-embed-text` at `http://localhost:11434/v1`
- Full results: `ai-steering/chunkhound/elixir-stress-test-results.md`
- PR target: `sandbox` → `main`, covers ChunkType.IMPORT fix + Elixir parser
- Code review notes: `ai-steering/chunkhound/code-review-notes.md`
- PR prep doc: `ai-steering/chunkhound/PR-chunktype-import.md`
