# Handover: chunkhound - 2026-03-06 10:46

## Current Task
ChunkType.IMPORT feature complete. PR prep and code review done. Ready to submit.

## Status
- [x] `ChunkType.IMPORT` committed and pushed (`f0e9f94`)
- [x] 3 test projects re-indexed — zero unknown chunks (CLP, FMR, CaNTa)
- [x] Smoke 17/17, Elixir parser 20/20
- [x] Upstream issue #215 filed (--db nested directory bug)
- [x] infranav-v2 re-indexing started by user (1,329 files, 37k+ chunks — check results)
- [x] Code review notes written
- [x] All docs in ai-steering/chunkhound/ committed and pushed
- Current branch: `sandbox`
- Last commit: `b19ec27`
- Working tree: clean

## Immediate Next Steps (before PR)
1. Run full test suite: `uv run pytest`
2. Run linter + typecheck: `uv run ruff check chunkhound && uv run mypy chunkhound`
3. Check infranav-v2 re-index results (zero unknown expected)
4. Decide: PR from `sandbox` or dedicated feature branch
5. Submit PR to `chunkhound/chunkhound` — template in `ai-steering/chunkhound/PR-chunktype-import.md`

## Follow-up Tasks (post-PR, from code review)
1. **Refactor `_map_concept_to_chunk_type`** — replace if/elif chain with dict lookup (`universal_parser.py:858-954`)
2. **Complete `hint_map`** — add missing types: enum, interface, trait, struct, variable, type, macro, etc.
3. **Fix Pyright type mismatches** — BaseMapping/LanguageMapping protocol, UniversalChunk/Chunk dedup types
4. **CLI flag precedence** — `--db`/`--config` should override project-local `.chunkhound.json`
5. **Unify Ollama provider naming** — accept `"ollama"` in map/research commands
6. **Clean unused code** — `_DOC_ATTR_KEYWORDS` in elixir.py, unused vars across files
7. **Investigate @spec/@type** low extraction in Elixir

## Key Docs (ai-steering/chunkhound/)
- `test-projects.md` — project registry with paths and before/after data
- `CaNTa-index-summary.md` — detailed CaNTa index stats
- `PR-chunktype-import.md` — PR description template
- `reindex-guide.md` — how to re-index + tree-sitter upgrade guide
- `code-review-notes.md` — 7 issues with file locations and fixes

## Context for Next Session
- CaNTa is sensitive — no details outside ai-steering/chunkhound/
- FMR = "Project B" in public docs
- All test project DBs have nested `chunks.db/chunks.db` path (issue #215)
- `claude-code-cli` synthesis can't nest — use separate terminal for map/research
