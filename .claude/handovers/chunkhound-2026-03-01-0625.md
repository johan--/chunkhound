# Handover: chunkhound - 2026-03-01 06:25

## Current Task
Adding Elixir language support to ChunkHound — completed planning and requirements spec, kiro pipeline in progress.

## Status
- [ ] Kiro pipeline in progress (spec drafted & approved, design + tasks still needed)
- Current branch: `sandbox`
- Last commit: `b68b613 - fix: remove spurious content arg from _validate_and_split_chunk call in _detect_embedded_sql (#202)`
- `.mcp.json` updated with LLM config (untracked, not committed)
- No code changes made yet — planning phase only

## Immediate Next Steps
1. **Write `requirements.md`** to `docs/ai-specs/elixir-language-support/requirements.md` — content is drafted (7 user stories, approved by user)
2. **Run kiro-design** — generate `design.md` with technical decisions
3. **Run kiro-tasks** — generate `tasks.md` with implementation tasks
4. **Execute tasks** — implement the Elixir parser

## Blockers / Open Questions
- Plan mode conflicted with kiro pipeline file writes — pipeline couldn't write spec files while plan mode was active. Workflow friction to address in skills.
- User noted `.heex`/`.leex` should be future follow-up with Vue/Svelte-style custom parser

## Key Decisions Made
- **Spec location:** `docs/ai-specs/elixir-language-support/`
- **File extensions:** `.ex` and `.exs` only (no `.heex`/`.leex` for now)
- **Template:** Use `lua.py` mapping as base template (closest functional-language analogue)
- **Import style:** Direct `import tree_sitter_elixir as ts_elixir` (standalone PyPI package, like Haskell)
- **Full kiro pipeline:** Spec → Design → Tasks → Execute
- **LLM config updated:** `.chunkhound.json` now has `utility_provider: ollama` (qwen3:8b) + `synthesis_provider: claude-code-cli`

## Context the Next Session Needs
- **Plan file:** `/home/johan/.claude/plans/glittery-roaming-zephyr.md` — full implementation plan with file list, concept mappings, metadata kinds
- **Tree-sitter-elixir grammar:** Local at `/home/johan/workbench/tools/tree-sitter-elixir` — key insight: everything is `call` nodes, keywords are `target: (identifier)` text
- **Approved requirements draft:** 7 user stories covering file detection, modules, functions/macros, types/specs, imports, comments, smoke tests — needs to be written to file
- **Key files to modify:** `pyproject.toml`, `common.py` (Language enum), `parser_factory.py`, `mappings/__init__.py`
- **Key file to create:** `chunkhound/parsers/mappings/elixir.py` (core work)
- **AST node mapping reference:** Elixir uses `call` for everything; function name is nested in `arguments > call > identifier`

## Running Processes
- None
