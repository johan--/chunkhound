# Handover: chunkhound - 2026-06-22 13:06

## Current Task
Land `.haml` text-fallback support in the fork (Option A), then decide on upstreaming.

## Status
- [x] Change implemented + committed (NOT merged to dev, NOT pushed)
- Current branch: `feat/haml-text-fallback`
- Last commit: `d16f153b - feat(parsers): index .haml files via text fallback`
- Working tree: clean (all 3 files committed)
- Branched from `dev` (which tracks `origin/dev`); `origin` = `johan--/chunkhound`, `upstream` = `chunkhound/chunkhound`

## The change (3 files, 9 insertions)
- `chunkhound/core/types/common.py` - `".haml": cls.TEXT` in `from_file_extension` extension_map
- `chunkhound/parsers/parser_factory.py` - `".haml": Language.TEXT` in `EXTENSION_TO_LANGUAGE`
- `tests/test_language_extensions.py` - `.haml` added to both coverage lists (`test_common_text_extensions_indexed`, `test_realtime_service_indexes_newly_supported_files`)
- Effect: `.haml` was resolving to `UNKNOWN` and being silently skipped (no discovery glob). Now indexed/searchable as TEXT (cAST chunks, no symbol extraction). Mirrors `.sass`/`.proto`/`.xml`.

## Verification done
- Smoke: 18 passed. Blast-radius: `test_parsers.py` + `test_language_extensions.py` = 141 passed.
- End-to-end detection: `detect_language`, `Language.from_file_extension`, `EXTENSION_TO_LANGUAGE['.haml']` all -> `TEXT`. Real `.haml` sample parses to a `paragraph` text chunk.
- Real-world: re-indexed `/home/johan/projects/apdp/apdp_v2` (Rails app) with this dev build -> 177 files processed, 0 errors. User confirmed HAML view content now returns in searches that previously returned nothing. Change verified on a real project.

## Immediate Next Steps
1. **Full-suite gate (the open verification).** The push gate `uv run pytest tests/ -v` ran: **17 failed, 3829 passed, 111 skipped, 52 errors** (207s). The 52 errors are ALL `tests/site/` (web build: configurator/site_build/theme_contrast/og_images/mobile_nav/hero_terminal) = baseline noise (no node/npm tooling). The 17 failure NAMES were not captured. Before pushing, do the baseline diff per [[chunkhound-baseline-gate-noise]]: run the suite on `origin/dev` (or merge-base) and confirm the 17 + 52 are all pre-existing and none touch parsers/language. My blast-radius tests are clean, so this is expected to pass - but confirm it.
2. **Then plan (a) finish:** `git switch dev && git merge feat/haml-text-fallback && git push origin dev`. **Needs explicit user go** - project config does not pre-authorize git writes (global git hard-wall). User reserved merge/push for their review.
3. **Upstreaming (separate track):** a maintainer Discord message was drafted this session (asking if they'd take a `.haml`->TEXT fallback PR). User is posting it. If maintainers say yes -> open PR against `upstream/main`. Independent of the fork-local landing above.

## Blockers / Open Questions
- Push to `origin/dev` needs explicit user authorization (not granted by project CLAUDE.md/AGENTS.md).
- Awaiting maintainer reply on Discord re: whether to upstream.
- The 17 full-suite failures are unconfirmed-against-baseline (see step 1).

## Key Decisions Made
- **Option A (TEXT fallback) over Option B (full structural HAML):** there is NO tree-sitter HAML grammar - `get_language("haml")` raises `LookupError` in tree-sitter-language-pack and no `tree_sitter_haml` module exists. Full parsing would require vendoring an external grammar + a `pip_package=` override, which breaks the "grammars are required deps / default `tree-sitter-{name}` formula" convention (only SCSS is exempt). TEXT fallback = ~3 lines, zero deps, exactly how `.sass` is handled.
- **Feature branch + local merge into `dev`** (matches how Ruby support landed - merge message was a plain `git merge`, not a GitHub PR merge). No GitHub PR needed on the user's own fork.
- TEXT-fallback extensions do NOT need the heavy coverage-guard test additions (LANGUAGE_SAMPLES etc.) - those only fire for new `Language` enum members. Only `test_language_extensions.py` parametrize lists needed entries.

## Context the Next Session Needs
- **Two extension maps must agree:** `common.py` `from_file_extension` AND `parser_factory.py` `EXTENSION_TO_LANGUAGE`. `core/detection/language_detector.py:detect_language` just delegates to `from_file_extension` (only `.m` gets content detection). The file walker builds discovery globs from `EXTENSION_TO_LANGUAGE` - a miss there = files never indexed.
- Run `chunkhound` from the repo dir so `uv run` resolves to THIS dev build (with the HAML change), not an installed release.
- apdp_v2 verification DB: `/home/johan/workbench/chunkhound-dbs/apdp-v2` (pinned in its `.chunkhound.json`, out of repo tree). A live chunkhound process may hold the DuckDB single-writer lock (hit a lock conflict on PID 384721 during read).
- The apdp_v2 code findings (AITF 3-toggle race, two warning/gate partials disagreeing) were VERIFICATION ONLY - user said "NO - we are doing it" = those are not chunkhound work; they belong in the user's apdp docs. Out of scope here.

## Running Processes
- Likely an MCP server / daemon holding the apdp-v2 DuckDB write lock (benign; restart its session if a re-index needs the lock).

## Cleanup note
- 4 old handovers in `.claude/handovers/` (Mar 14, Mar 18, May 11, Jun 15). Mar/May ones are >30 days - candidates for cleanup next session start.
