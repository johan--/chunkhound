# Handover: chunkhound - 2026-05-11 09:11

## Current Task
Docs/notes cleanup after the `~/workbench/tools/chunkhound` → `~/projects/nxt-level-systems/chunkhound` move + correcting AGENTS.md DB layout claims to match reality.

## Status
- Current branch (chunkhound): `dev`
- Last commit (chunkhound): `c6add6a4 Merge remote-tracking branch 'upstream/main' into dev` (unpushed)
- Working tree (chunkhound): ` M AGENTS.md` (uncommitted)
- Working tree (ops-deck `/home/johan/workbench/ops-deck`): ~12 files modified across `chunkhound/`, `claude-code/claude-configs/skills/hound-init/`, `codeindex/planning/`, `melangeX/ellienet-jvr/ellienet/CLAUDE.md`, plus unrelated dirty files (deleted plans, `sofo/project-admin/wa_notes_10_may_2026.txt`)

## Immediate Next Steps
1. **Push chunkhound `dev`** to `origin` if user wants the upstream merge visible on the fork (`git push origin dev`). Not pushed yet.
2. **Commit + push the ops-deck doc changes** — they live in a different repo and weren't committed this session. Scope it to the chunkhound/hound-init/codeindex/melangeX edits; the unrelated dirty files (deleted plans, wa_notes) should not be bundled.
3. **Finish the original task** — set up ChunkHound for `/home/johan/projects/sofo/fmr/fmr-ui`. The user pivoted to docs cleanup before `.chunkhound.json` was written. fmr-ui is already indexed (745M DB) per the user's last verification, so this may now just be writing the config + `.gitignore` entry.

## Blockers / Open Questions
- None blocking. Permission for commit/push on chunkhound and ops-deck wasn't explicitly granted in CLAUDE.md/AGENTS.md, so confirm before pushing.

## Key Decisions Made
- **Upstream sync via `merge upstream/main` into `dev`** (not via local `main`) — continues the prior pattern (commit 973c0274). User confirmed.
- **Treated stale chunkhound handovers as worth path-updating** rather than deleting, despite global handover-hygiene guidance. User asked to "update docs, notes, etc." — interpreted "notes" inclusively.
- **Preserved one historical reference** to `~/workbench/tools/chunkhound` in `ops-deck/.claude/handovers/ops-deck-2026-04-23-0853.md:17` because it's the literal `mv` migration command, not a stale forward-looking pointer.
- **Removed the post-index `duckdb.connect()` verify section** from `reindex-guide.md` (it used the wrong path `.chunkhound/db/chunks.db` — the real layout is flat). User confirmed "not working".
- **AGENTS.md DB_PATH_GOTCHAS rewrite** — the "Default DB path: `.chunkhound/db/chunks.db` (directory structure, not flat file)" claim was wrong. Real layout is `<dir-you-passed>/chunks.db` + `chunks.db.root.json` sidecar. Verified against fmr-core (7.1G) and fmr-ui (745M) which both have the flat layout.
- **Historical doc artifacts annotated, not rewritten** — `test-projects.md` "DB Path Notes" and `CaNTa-index-summary.md` line 68 both reflect the March 2026 `--db` nested-dir bug state. Added contextual notes pointing readers to the current flat-layout reality rather than deleting historical record.

## Context the Next Session Needs

### Files changed this session

**chunkhound repo (uncommitted):**
- `AGENTS.md` — `DB_PATH_GOTCHAS` section rewritten (lines ~102-107)

**ops-deck repo (uncommitted):**
- `claude-code/claude-configs/skills/hound-init/SKILL.md` — path refs (3) + new bullet about stale `database.path` after moves
- `chunkhound/reindex-guide.md` — path refs, removed verify section, added "After moving or renaming a project" section with FMR worked example, fixed `lsof` paths to point at `chunks.db`
- `chunkhound/mcp-config-guide.md` — path refs
- `chunkhound/elixir-search-tests.md` — path ref + fixed `duckdb.connect()` path
- `chunkhound/test-projects.md`, `chunkhound/CaNTa-index-summary.md` — annotated historical DB-path notes
- `codeindex/planning/{PLAN-librarian-intelligence-layer,ROADMAP-v2}.md` — path refs
- `melangeX/ellienet-jvr/ellienet/CLAUDE.md` — path refs
- Several handovers under `chunkhound/handovers/` and `chunkhound/.claude/handovers/` — path refs

**Live configs updated (would have broken):**
- 5 `.mcp.json` in `~/workbench/elixir/{absinthe,ecto,phoenix,oban,swoosh}/` — pointed to old venv
- 2 `.gemini/settings.json` in `~/projects/hipconsult/infranav-v2{,-tests}/`
- 1 handover in `~/workbench/tools/repowise/.claude/handovers/`

### Key facts to carry forward

- **ChunkHound flat DB layout (corrected May 2026):** Whatever directory you pass via `--db` or `database.path` gets `chunks.db` (flat DuckDB file) + `chunks.db.root.json` (sidecar) written inside it. There is NO `db/` subdirectory by default. References to `.chunkhound/db/chunks.db` in older handovers are stale.
- **`--db` still has the "file path" gotcha:** passing `--db .../chunks.db` creates a nested `chunks.db/chunks.db` directory (issue #215, still open). Pass the parent dir.
- **fmr-core DB was 7.1G / 14392 chunks**, fmr-ui 745M — both healthy after the rename+reindex.
- **Authoritative health signal is the indexer's own report** (Files / Chunks / Errors / Time). External `duckdb.connect()` verification is unnecessary paranoia and was the source of broken doc commands.

## Running Processes
- None.
