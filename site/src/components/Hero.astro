---
import Wordmark from "./Wordmark.astro";
import { COPY_SVG, DEMO_CODE_PALETTE } from "../lib/shiki-config.js";
---

<section class="hero">
    <div class="container">
        <h1 class="headline"><Wordmark size="lg" layout="stacked" /></h1>
        <p class="tagline">Your entire engineering context, deeply understood</p>
        <p class="subheadline">
            Open-source codebase intelligence that gives agents and teams cited context across current code, git history, and technical web research.
        </p>
        <!-- Keep in sync with README.md amplifier line -->
        <p class="amplifier">
            Local-first · Dozens of languages & file types · Cited answers · Git history research · Pinpoint web research
        </p>

        <div class="cta-row">
            <div class="install-pill">
                <code class="install-cmd">uv tool install chunkhound</code>
                <button
                    type="button"
                    class="copy-btn"
                    onclick="navigator.clipboard.writeText('uv tool install chunkhound')"
                    aria-label="Copy install command"
                    set:html={COPY_SVG}
                />
            </div>
            <a
                href="https://github.com/chunkhound/chunkhound"
                class="github-btn"
            >
                <svg
                    class="github-icon"
                    width="16"
                    height="16"
                    viewBox="0 0 16 16"
                    fill="currentColor"
                >
                    <path
                        d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.01 8.01 0 0016 8c0-4.42-3.58-8-8-8z"
                    ></path>
                </svg>
                <span data-star-count>--</span>
                <svg
                    class="star-icon"
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                    stroke="none"
                >
                    <path
                        d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"
                    ></path>
                </svg>
            </a>
        </div>

        <div class="terminal-card">
            <div class="terminal-dots">
                <span class="dot red"></span>
                <span class="dot yellow"></span>
                <span class="dot green"></span>
            </div>
            <div class="terminal-lines" id="terminal-lines"></div>
        </div>

        <blockquote class="trust-quote">
            &ldquo;We&rsquo;ve repeatedly demonstrated compressing what used to take
            2&ndash;3 months down to 2&ndash;3 days. Claude Code orchestrates
            tens to hundreds of deep research calls over our monorepo, zooming
            into each subsection systematically.&rdquo;
            <cite
                >— Ofir Rozenfeld, AI Transformation Product Manager at <a
                    href="https://www.appliedmaterials.com"
                    target="_blank"
                    rel="noopener">Applied Materials</a
                ></cite
            >
        </blockquote>
    </div>
</section>

<script>
    import "../scripts/hero-terminal.ts";
</script>

<style
    define:vars={{
        themeCmd: DEMO_CODE_PALETTE.cmd,
        themeString: DEMO_CODE_PALETTE.string,
    }}
>
    .hero {
        padding: var(--space-10) 0;
        text-align: center;
    }

    .trust-quote {
        margin: var(--space-8) auto 0;
        max-width: 560px;
        padding: 0 var(--space-4);
        text-align: center;
        font-size: var(--text-sm);
        line-height: var(--lh-sm);
        font-weight: 400;
        color: var(--text-secondary);
        font-style: italic;
    }

    .trust-quote::before {
        content: "\201C";
        display: block;
        font-size: 3rem;
        line-height: 1;
        font-weight: 700;
        font-style: normal;
        color: var(--code-accent);
        margin-bottom: var(--space-2);
    }

    .trust-quote cite {
        display: block;
        margin-top: var(--space-2);
        font-style: normal;
        font-weight: 600;
        font-size: var(--text-xs);
        letter-spacing: 0.04em;
        color: var(--text-tertiary);
    }

    .trust-quote a {
        color: var(--link);
        text-decoration: none;
    }

    .trust-quote a:hover {
        color: var(--link-hover);
        text-decoration: underline;
    }

    .amplifier {
        font-size: var(--text-sm);
        line-height: var(--lh-sm);
        font-weight: 500;
        color: var(--text-secondary);
        letter-spacing: 0.02em;
        margin-bottom: var(--space-6);
    }

    .headline {
        margin-bottom: var(--space-2);
    }

    .tagline {
        font-size: var(--text-xl);
        line-height: var(--lh-xl);
        font-weight: 600;
        color: var(--text-secondary);
        margin-bottom: var(--space-4);
    }

    .subheadline {
        font-size: var(--text-lg);
        line-height: var(--lh-lg);
        font-weight: 500;
        color: var(--text-secondary);
        max-width: 640px;
        margin: 0 auto var(--space-6);
    }

    .cta-row {
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
        gap: var(--space-3);
        flex-wrap: wrap;
    }

    .install-pill {
        background: var(--primary);
        border-radius: var(--radius-full);
        padding: var(--space-2) var(--space-5);
        display: flex;
        align-items: center;
        gap: var(--space-2);
    }

    .install-cmd {
        font-family: "JetBrains Mono", monospace;
        font-size: 14px;
        color: var(--on-primary);
        white-space: nowrap;
    }

    .copy-btn {
        background: none;
        border: none;
        color: var(--on-primary);
        cursor: pointer;
        padding: 0;
        display: flex;
        align-items: center;
        flex-shrink: 0;
        opacity: 0.8;
        transition: opacity 0.2s;
    }
    .copy-btn:hover {
        opacity: 1;
    }

    .copy-btn :global(svg) {
        width: 18px;
        height: 18px;
    }

    .github-btn {
        border: var(--border-w-default) solid var(--border-color);
        border-radius: var(--radius-full);
        padding: var(--space-2) var(--space-5);
        color: var(--text-primary);
        font-size: 14px;
        font-weight: 500;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: var(--space-2);
    }

    .github-btn:hover {
        text-decoration: none;
    }

    @media (max-width: 360px) {
        .install-pill {
            gap: var(--space-1);
            padding: var(--space-2) var(--space-4);
        }

        .install-cmd {
            font-size: 12px;
        }
    }

    .github-icon {
        flex-shrink: 0;
    }

    .star-icon {
        color: var(--primary-bright);
        flex-shrink: 0;
    }

    .terminal-card {
        background: var(--code-bg);
        border-radius: var(--radius-md);
        padding: var(--space-5);
        max-width: 600px;
        margin: var(--space-8) auto 0;
        text-align: left;
    }

    .terminal-dots {
        display: flex;
        gap: var(--space-2);
        margin-bottom: var(--space-4);
    }

    .dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
    }
    .dot.red {
        background: #ff5f56;
    }
    .dot.yellow {
        background: #ffbd2e;
    }
    .dot.green {
        background: #27c93f;
    }

    .terminal-lines {
        font-family: "JetBrains Mono", monospace;
        font-size: 14px;
        line-height: 1.4;
    }

    /* Terminal content — uses :global() so JS-injected elements match */
    .terminal-lines :global(.prompt) {
        color: var(--text-muted);
    }
    .terminal-lines :global(.cmd) {
        color: var(--themeCmd);
    }
    .terminal-lines :global(.output) {
        color: var(--code-text);
    }
    .terminal-lines :global(.dim) {
        opacity: 0.6;
    }
    .terminal-lines :global(.str) {
        color: var(--themeString);
    }

    .terminal-lines :global(.cursor) {
        display: inline-block;
        width: 2px;
        height: 16px;
        background: var(--primary-bright);
        margin-left: 4px;
        vertical-align: text-bottom;
        animation: blink 1s step-end infinite;
    }

    @keyframes blink {
        0%,
        100% {
            opacity: 1;
        }
        50% {
            opacity: 0;
        }
    }
</style>
