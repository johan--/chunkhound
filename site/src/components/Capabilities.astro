---
import Chip from "./Chip.astro";

const capabilities = [
  {
    icon: "ph-graph",
    title: "Multi-Hop Semantic Search",
    description: "Follows semantic bridges across files and subsystems. Three hops surface architectural relationships that plain text matching misses.",
    tag: "3-hop traversal",
  },
  {
    icon: "ph-detective",
    title: "Gap Detection & Filling",
    description: "Finds missing parts of the answer by clustering evidence, spotting uncovered concepts, and filtering noise before synthesis.",
    tag: "Elbow detection",
  },
  {
    icon: "ph-tree-structure",
    title: "cAST Chunking",
    description: "Code-aware chunking from Carnegie Mellon research that improves retrieval quality before search and research even begin.",
    tag: "arXiv:2506.15655",
    tagUrl: "https://arxiv.org/abs/2506.15655",
  },
  {
    icon: "ph-compass",
    title: "Depth Exploration",
    description: "Explores promising files from multiple angles, so one subsystem can be understood by behavior, responsibilities, and patterns.",
    tag: "Aspect-based queries",
  },
  {
    icon: "ph-file-magnifying-glass",
    title: "Code Research",
    description: "One call produces a cited markdown explanation of how the system works, grounded in the files and components behind the answer.",
    tag: "Cited reports",
  },
  {
    icon: "ph-intersect-three",
    title: "Unified Semantic + Regex",
    description: "Combines conceptual discovery with exact symbol and regex tracing, then reranks everything against the original question.",
    tag: "Hybrid search",
  },
  {
    icon: "ph-clock-counter-clockwise",
    title: "Git History Research",
    description: "Asks by last N commits, commit hash, tag, branch, or range to explain large PRs, release ranges, and why behavior changed.",
    tag: "--last-n · --commit-range · --commit-hash",
  },
  {
    icon: "ph-globe-hemisphere-west",
    title: "Pinpoint Web Research",
    description: "Finds the cited external docs, APIs, issues, and articles your implementation depends on, then brings them into agent context.",
    tag: "Cited URL research",
  },
  {
    icon: "ph-book-open-text",
    title: "Auto-Documentation",
    description: "Turns research-backed understanding into a searchable documentation site generated directly from the codebase.",
    tag: "5 levels",
  },
];
---

<section id="capabilities" class="capabilities">
  <div class="container">
    <p class="eyebrow">CAPABILITIES</p>
    <h2 class="heading">Capabilities behind deep codebase understanding</h2>

    <div class="grid">
      {capabilities.map((cap) => {
        const Icon = cap.icon;
        return (
          <div class="card">
            <Icon class="card-icon" size="28" weight="duotone" aria-hidden="true" />
            <h3 class="card-title">{cap.title}</h3>
            <p class="card-desc">{cap.description}</p>
            <Chip href={cap.tagUrl}>{cap.tag}</Chip>
          </div>
        );
      })}
    </div>

    <blockquote class="cap-quote">
      &ldquo;Chunkhound has been an absolute beast in reading multiple large repos in such extents that would take a human days or the plain claude code setup multiple attempts to get it (at least somewhat) right.&rdquo;
      <cite>— @flrk, co-founder of <a href="https://kimara.ai/" target="_blank" rel="noopener">Kimara AI</a></cite>
    </blockquote>
  </div>
</section>

<script>
  import "@phosphor-icons/webcomponents/PhBookOpenText";
  import "@phosphor-icons/webcomponents/PhClockCounterClockwise";
  import "@phosphor-icons/webcomponents/PhCompass";
  import "@phosphor-icons/webcomponents/PhDetective";
  import "@phosphor-icons/webcomponents/PhFileMagnifyingGlass";
  import "@phosphor-icons/webcomponents/PhGlobeHemisphereWest";
  import "@phosphor-icons/webcomponents/PhGraph";
  import "@phosphor-icons/webcomponents/PhIntersectThree";
  import "@phosphor-icons/webcomponents/PhTreeStructure";
</script>

<style>
  .capabilities {
    padding: var(--space-10) 0;
  }

  .container {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  .eyebrow {
    text-transform: uppercase;
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.12em;
    color: var(--text-tertiary);
    text-align: center;
    margin-bottom: var(--space-3);
  }

  .heading {
    font-size: var(--text-2xl);
    line-height: var(--lh-2xl);
    font-weight: 600;
    color: var(--text-primary);
    text-align: center;
    margin-bottom: var(--space-5);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-5);
  }

  .card {
    background: var(--bg-surface);
    border: var(--border-w-default) solid var(--border-color);
    border-radius: var(--radius-xl);
    padding: var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .card:last-child:nth-child(odd) {
    grid-column: 1 / -1;
    max-width: calc(50% - var(--space-5) / 2);
    justify-self: center;
  }

  .card-icon {
    color: var(--primary);
    margin-bottom: var(--space-1);
  }

  .card-title {
    font-size: var(--text-lg);
    line-height: var(--lh-lg);
    font-weight: 600;
    color: var(--text-primary);
  }

  .card-desc {
    font-size: var(--text-sm);
    line-height: var(--lh-sm);
    color: var(--text-secondary);
    flex: 1;
  }

  .cap-quote {
    margin: var(--space-6) auto 0;
    max-width: 720px;
    padding: 0 var(--space-5);
    text-align: center;
    font-size: var(--text-base);
    line-height: var(--lh-base);
    font-weight: 400;
    color: var(--text-secondary);
    font-style: italic;
  }

  .cap-quote::before {
    content: '\201C';
    display: block;
    font-size: 3rem;
    line-height: 1;
    font-weight: 700;
    font-style: normal;
    color: var(--code-accent);
    margin-bottom: var(--space-2);
  }

  .cap-quote cite {
    display: block;
    margin-top: var(--space-2);
    font-style: normal;
    font-weight: 600;
    font-size: var(--text-xs);
    letter-spacing: 0.04em;
    color: var(--text-tertiary);
  }

  .cap-quote a {
    color: var(--link);
    text-decoration: none;
  }

  .cap-quote a:hover {
    color: var(--link-hover);
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .grid {
      grid-template-columns: 1fr;
    }

    .card:last-child:nth-child(odd) {
      max-width: none;
      grid-column: auto;
    }
  }
</style>
